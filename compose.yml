services:
  web:
    build:
      context: .
      dockerfile: ./Dockerfile
    image: healing-hearts-minds-bot
    container_name: healing-hearts-minds-bot
    hostname: healing-hearts-minds-bot
    entrypoint: /usr/local/bin/entrypoint.sh
    env_file: ./.env
    tty: true
    volumes:
      - ./src:/code/src
